.env
data/
*.db
*.db-shm
*.db-wal
__pycache__/
*.pyc
*.egg-info/
.DS_Store
.venv/
.worktrees/

# Benchmark ground truth is corpus, not transient state - keep it tracked
!data/beehiiv_fw_ground_truth.json
!data/beehiiv_fw_ground_truth_ANALYSIS.md

# Never commit env backups - they carry live API keys (blocked by GitHub push
# protection 10 Aug 2026 after a git add -A swept one in).
.env.*
*.backup.xlsx
